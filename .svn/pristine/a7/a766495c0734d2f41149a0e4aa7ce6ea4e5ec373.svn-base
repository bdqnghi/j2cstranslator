/*
 *******************************************************************************
 * Copyright (C) 2007, International Business Machines Corporation and         *
 * others. All Rights Reserved.                                                *
 *******************************************************************************
 */
// --------------------------------------------------------------------------------------------------
// This file was automatically generated by J2CS Translator (http://j2cstranslator.sourceforge.net/). 
// Version 1.3.6.20101125_01     
// 12/13/10 4:01 PM    
// ${CustomMessageForDisclaimer}                                                                             
// --------------------------------------------------------------------------------------------------
 namespace IBM.ICU.Charset {
	
	using IBM.ICU.Util;
	using System;
	using System.Collections;
	using System.Collections.Generic;
	using System.ComponentModel;
	using System.IO;
	using System.Runtime.CompilerServices;
	
	public class CalendarFieldsSet : FieldsSet {
	
	    
	    /// <param name="whichEnum"></param>
	    /// <param name="fieldsCount"></param>
	    public CalendarFieldsSet() : base(IBM.ICU.Charset.DebugUtilitiesData.UCalendarDateFields, -1) {
	    }
	
	    public bool Matches(Calendar cal, CalendarFieldsSet diffSet) {
	        bool match = true;
	        for (int i = 0; i < FieldCount(); i++) {
	            if (IsSet(i)) {
	                int calVal = cal.Get(i);
	                if (calVal != Get(i)) {
	                    match = false;
	                    diffSet.Set(i, calVal);
	                }
	            }
	        }
	        return match;
	    }
	
	    /// <summary>
	    /// set the specified fields on this calendar. Doesn't clear first. Returns
	    /// any errors the cale
	    /// </summary>
	    ///
	    public void SetOnCalendar(Calendar cal) {
	        for (int i = 0; i < FieldCount(); i++) {
	            if (IsSet(i)) {
	                cal.Set(i, Get(i));
	            }
	        }
	    }
	
	    protected internal override void HandleParseValue(FieldsSet inheritFrom, int field,
	            String substr) {
	        if (field == IBM.ICU.Util.Calendar.MONTH) {
	            ParseValueEnum(IBM.ICU.Charset.DebugUtilitiesData.UCalendarMonths, inheritFrom,
	                    field, substr);
	            // will fallback to default.
	        } else {
	            ParseValueDefault(inheritFrom, field, substr);
	        }
	    }
	}
}
