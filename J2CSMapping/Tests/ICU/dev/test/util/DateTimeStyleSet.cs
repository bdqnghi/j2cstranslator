/*
 *******************************************************************************
 * Copyright (C) 2007, International Business Machines Corporation and         *
 * others. All Rights Reserved.                                                *
 *******************************************************************************
 */
// --------------------------------------------------------------------------------------------------
// This file was automatically generated by J2CS Translator (http://j2cstranslator.sourceforge.net/). 
// Version 1.3.6.20101125_01     
// 12/13/10 4:01 PM    
// ${CustomMessageForDisclaimer}                                                                             
// --------------------------------------------------------------------------------------------------
 namespace IBM.ICU.Charset {
	
	using System;
	using System.Collections;
	using System.Collections.Generic;
	using System.ComponentModel;
	using System.IO;
	using System.Runtime.CompilerServices;
	
	
	public class DateTimeStyleSet : FieldsSet {
	
	    private const int DTS_DATE = 0;
	
	    private const String kDATE = "DATE";
	
	    private const int DTS_TIME = 1;
	
	    private const String kTIME = "TIME";
	
	    private const int DTS_COUNT = 2;
	
	    private const String kRELATIVE_ = "RELATIVE_";
	
	    private int GetOrNone(int which) {
	        if (!IsSet(which)) {
	            return IBM.ICU.Text.DateFormat.NONE;
	        } else {
	            return Get(which);
	        }
	    }
	
	    public DateTimeStyleSet() : base(IBM.ICU.Charset.FieldsSet.NO_ENUM, DTS_COUNT) {
	    }
	
	    public int GetDateStyle() {
	        return GetOrNone(DTS_DATE);
	    }
	
	    public int GetTimeStyle() {
	        return GetOrNone(DTS_TIME);
	    }
	
	    protected internal override void HandleParseValue(FieldsSet inheritFrom, int field,
	            String substr) {
	        if (substr.StartsWith(kRELATIVE_)) {
	            ParseValueEnum(IBM.ICU.Charset.DebugUtilitiesData.UDateFormatStyle, inheritFrom,
	                    field, substr.Substring(kRELATIVE_.Length));
	            if (IsSet(field)) {
	                Set(field, Get(field) | IBM.ICU.Text.DateFormat.RELATIVE);
	            }
	        } else {
	            ParseValueEnum(IBM.ICU.Charset.DebugUtilitiesData.UDateFormatStyle, inheritFrom,
	                    field, substr);
	        }
	    }
	
	    protected internal override int HandleParseName(FieldsSet inheritFrom, String name,
	            String substr) {
	        if (name.Equals(kDATE)) {
	            return DTS_DATE;
	        } else if (name.Equals(kTIME)) {
	            return DTS_TIME;
	        } else {
	            throw new ArgumentException("Bad field: " + name);
	        }
	    }
	}
}
